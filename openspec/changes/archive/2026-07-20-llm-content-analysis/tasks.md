## 1. 分析契约与项目基础

- [x] 1.1 新增 `@openai/codex-sdk` 依赖和 `src/analysis/` 模块结构，并确保服务端构建可以打包或外置所需运行时
- [x] 1.2 定义 `AnalysisProvider`、请求、响应、可用性、运行状态、用量和错误分类 TypeScript 契约
- [x] 1.3 使用 Zod 定义 `work-context/analysis@1` 可判别联合，并生成两个 Provider 共用的 JSON Schema
- [x] 1.4 为工作区增加 `config/analysis.md`、`.context/analysis/runs/` 和基础状态文档，保证初始化幂等且不写入凭证

## 2. Prompt 与结果校验

- [x] 2.1 实现 `context-analysis@1` 版本化 Prompt 构建器，包含角色、分类定义、信任边界、用户与时区、证据、日期和纯 JSON 输出规则
- [x] 2.2 对来源记录进行最小化和长度限制，使用随机分隔标识封装不可信 JSON 载荷，并计算 Prompt 内容哈希
- [x] 2.3 实现结果解析与领域校验，验证证据来自原文、参与者和来源 ID 有效、日期与枚举合法，并原子拒绝整批无效结果
- [x] 2.4 实现基于来源、结果类型和规范化证据的分析项键，以及重新分析时匹配、新增、过时和用户字段保护逻辑
- [x] 2.5 添加中文、英文、隐含行动、否定、多事项、无结果、含糊日期和提示注入固定样例与 Prompt 快照测试

## 3. Codex SDK Provider

- [x] 3.1 实现可注入的 `CodexSdkProvider`，通过独立空目录、只读沙箱、禁用 Web 搜索、无批准策略和超时启动新线程
- [x] 3.2 使用 `thread.run(prompt, { outputSchema, signal })` 获取最终响应、事件和用量，并映射为统一 Provider 响应
- [x] 3.3 检查 SDK 返回的事件，拒绝命令、文件修改、MCP、Web 搜索和其他非预期工具活动
- [x] 3.4 添加 SDK 可用性、结构化响应、取消、超时、认证失败、工具事件和无效结果的注入式契约测试

## 4. Codex Exec Provider

- [x] 4.1 实现可注入的 `CodexExecRunner`，使用参数数组和标准输入调用 `codex exec`，禁止 shell 字符串拼接
- [x] 4.2 使用临时空目录、`--ephemeral`、只读沙箱、`--json`、`--output-schema`、`--output-last-message`、超时和输出限制
- [x] 4.3 解析最终响应与 JSONL 事件，收集用量和脱敏诊断，并拒绝非预期工具活动、非零退出或不完整输出
- [x] 4.4 添加 CLI 缺失、参数生成、认证失败、超时、取消、输出过大、临时文件清理和有效结构化结果的契约测试

## 5. 分析协调与同步集成

- [x] 5.1 实现 Provider 注册表、有效配置解析和按运行快照选择 Provider，默认使用 `codex-sdk` 且不做静默故障转移
- [x] 5.2 实现分析协调器、稳定运行键、成功去重、尝试次数、脱敏运行记录和有界状态保留
- [x] 5.3 实现 Todo 与知识派生写入器，支持一条来源的多个结果、来源依据、分析元数据和混合文档用户字段保护
- [x] 5.4 重构飞书同步为先持久化来源和人物，再调用异步分析；分析失败不得阻止来源检查点推进
- [x] 5.5 保留飞书原生任务的确定性权威映射，删除消息分类用的 `ACTION_PATTERN`、`STRONG_ACTION_PATTERN`、`DECISION_PATTERN` 和固定置信度逻辑
- [x] 5.6 实现单条来源和有界时间范围的手动重试/重新分析服务，不在启动时执行无界历史扫描

## 6. 配置、API 与 Web 界面

- [x] 6.1 实现工作区分析配置读取、原子更新和 `CONTEXT_SPACE_ANALYSIS_PROVIDER` 环境覆盖，并在覆盖生效时锁定 UI 编辑
- [x] 6.2 扩展本地配置 API，返回已注册 Provider、可用性、当前选择、配置来源、Prompt 版本和最近运行状态
- [x] 6.3 添加 Provider 切换和重新分析 API，验证输入并保证切换只影响新运行且不会隐式发起测试调用
- [x] 6.4 在 Settings 增加 `codex-sdk` / `codex-exec` 选择、可用性、错误、隐私提示和环境锁定状态
- [x] 6.5 在 Todo、知识候选和同步状态中展示 Provider、Prompt 版本、分析时间、置信度与来源引用，不展示凭证或内部推理

## 7. 安全、集成验证与文档

- [x] 7.1 为两个 Provider 实施最小环境变量白名单、隔离目录、只读权限、禁用搜索/MCP 和临时资源清理，并验证凭证不会进入 Markdown 或日志
- [x] 7.2 添加端到端集成测试，覆盖 Provider 切换、运行中配置快照、无静默回退、来源同步失败隔离、幂等重试和派生协调
- [x] 7.3 更新 README 和 `.env.example`，说明两种调用方式的安装、认证、切换、隐私、SDK 会话持久化差异和显式冒烟测试
- [x] 7.4 运行 OpenSpec 严格校验、类型检查、代码检查、全部自动化测试和生产构建，并确认常规测试不会访问真实 LLM
