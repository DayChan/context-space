## 1. 单页适配器契约

- [x] 1.1 为消息来源返回标准化的分页元数据，并支持传入下一页游标
- [x] 1.2 将消息命令改为单页查询，移除 `--page-all` 并增加 `--no-reactions`

## 2. 同步编排与完整性

- [x] 2.1 在同步服务中逐页拉取并在请求下一页前持久化成功记录
- [x] 2.2 校验缺失游标、重复游标和每窗口安全页数上限，失败时保留已写入页面且不推进检查点
- [x] 2.3 增加不包含游标或正文的页级结构化日志

## 3. 自动化验证

- [x] 3.1 更新命令构造测试，验证单页参数、下一页游标和禁用表情富化
- [x] 3.2 增加多页 P2P 完整采集与超过四十页仍继续的测试
- [x] 3.3 增加分页中断后幂等回放、异常游标和安全页数上限测试
- [x] 3.4 运行类型检查、Lint、测试和构建

## 4. 使用文档

- [x] 4.1 更新 README 中的飞书分页、失败恢复和完整性说明
