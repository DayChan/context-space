## MODIFIED Requirements

### Requirement: 相关上下文采集
适配器 SHALL 采集提及用户的群消息、P2P 对话双方的消息、配置滚动窗口内的日历日程，以及分配给用户的任务，并将标准化来源写入 SQLite。

#### Scenario: 同步所有已启用的飞书来源
- **WHEN** 所有飞书命令都返回有效记录
- **THEN** 系统在 SQLite 中写入标准化提及、P2P、日历和任务来源，并使用提供方范围内稳定的来源 ID

### Requirement: 增量且幂等的同步
适配器 SHALL 使用 SQLite 游标、重叠窗口、分页和稳定来源 ID，确保重复运行不会创建重复来源或重复分析任务。

#### Scenario: 重复执行存在重叠的运行
- **WHEN** 第二次运行返回第一次已经采集的记录，并额外返回一条新记录
- **THEN** 系统更新或跳过现有记录，只创建一条新来源，并只为需要分析的内容幂等入队

### Requirement: 失败隔离与状态
适配器 SHALL 在 SQLite 中按同步运行及来源报告成功、失败、数量、时间戳和可操作错误，同时保留之前有效的游标；同步结果 MUST 与异步分析结果分开表示。

#### Scenario: 日历获取失败
- **WHEN** 消息和任务同步成功，但日历命令失败
- **THEN** 系统提交成功来源及其分析任务、展示日历失败，并且不推进日历游标

#### Scenario: 分析 Provider 不可用
- **WHEN** 来源已可靠提交但异步分析随后失败
- **THEN** 同步运行仍保持采集成功，分析任务独立展示失败并允许重试
