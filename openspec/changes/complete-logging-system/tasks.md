## 1. 日志核心

- [x] 1.1 定义 Logger 接口、日志等级、稳定事件信封、子 Logger 和 `AsyncLocalStorage` 关联上下文
- [x] 1.2 实现递归字段净化、Error 序列化、字符串凭证替换及大小与循环引用保护
- [x] 1.3 实现环境变量配置解析、等级过滤、终端目标和测试环境静默默认值
- [x] 1.4 实现 JSONL 文件写入队列、UTC 日期与大小轮转、保留期清理、权限及目标故障降级
- [x] 1.5 为日志核心补充结构化格式、过滤、上下文隔离、脱敏、轮转、保留和降级测试

## 2. 服务与请求链路

- [x] 2.1 在 `createApp` 中注入 Logger，并为初始化、HTTP 完成、中断和全局错误增加关联日志
- [x] 2.2 在服务入口记录启动、监听、停止、进程级异常，接入安全关闭与日志刷新
- [x] 2.3 补充请求编号复用与生成、并发隔离、错误响应和不记录查询值或请求体的 API 测试

## 3. 飞书同步链路

- [x] 3.1 为 `LarkCliCommandRunner` 增加命令族、参数名、耗时、输出大小和结构化 issue 日志
- [x] 3.2 为 `LarkSyncService` 增加同步、来源、窗口、持久化、批量分析和最终摘要事件
- [x] 3.3 补充成功同步、权限或参数错误以及分析失败事件的日志测试

## 4. LLM 分析链路

- [x] 4.1 为分析协调器增加批次选择、跳过、Provider 可用性、调用、验证、写入和失败日志
- [x] 4.2 将 Codex Exec 的脱敏 diagnostic 作为独立 warning 事件持久化，并记录安全的 usage 与事件类型
- [x] 4.3 补充分析成功、跳过、diagnostic、非法工具事件与无效输出的日志测试

## 5. 文档与整体验证

- [x] 5.1 更新 README，说明日志目录、环境变量、JSON 字段、常用查询、关闭方式、隐私边界和单进程限制
- [x] 5.2 运行类型检查、Lint、自动化测试、构建和 OpenSpec 校验并修复发现的问题
