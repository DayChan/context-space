## 1. 权限预检基础设施

- [x] 1.1 定义最小 scope、结构化预检状态、固定参数 CLI 检查器和错误分类
- [x] 1.2 为权限齐全、缺失 scope、认证失败、CLI 缺失和非法输出增加单元测试

## 2. 同步边界与 API

- [x] 2.1 增加完整成功同步判定，并在同步服务中实现每次预检、首次缺权前置阻断和后续兼容
- [x] 2.2 增加预检查询/刷新 API 与手动、定时同步回归测试，验证阻断时无运行、游标和业务命令副作用

## 3. Settings 交互

- [x] 3.1 在飞书来源区域展示权限状态、最小 scope、缺失 scope、授权命令和重新检查入口
- [x] 3.2 首次同步未就绪时阻止同步，完整同步后缺权只告警，并增加前端回归测试

## 4. 文档与验证

- [x] 4.1 更新 README 的首次同步权限准备与人工授权说明
- [x] 4.2 执行 OpenSpec 严格校验、类型检查、Lint、完整测试和生产构建
